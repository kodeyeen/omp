// This code is generated by codegen command. Do not edit.
// As we cannot directly call a C function pointer from Go code
// we need to create C wrapper functions around ompcapi pointers
// which will be used by Go code.

#include "include/cwrappers.h"

struct OMPAPI_t* ompapi;

{{- range .}}

// {{.Name}} functions
{{range .Funcs}}
{{.Ret}} {{.Name}}({{range $i, $e := .Params}}{{if $i}}, {{end}}{{$e.Type}} {{$e.Name}}{{end}}) {
    {{- $splitName := split .Name "_"}}
    return ompcapi->{{join $splitName "."}}({{range $i, $e := .Params}}{{if $i}}, {{end}}{{$e.Name}}{{end}});
}
{{end}}

{{- end -}}
